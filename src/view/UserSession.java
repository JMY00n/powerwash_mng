package view;

public class UserSession {
    // 현재 로그인된 사용자 정보 (static으로 전역 접근 가능)
    public static String EMP_ID = "";      // 사번 (초기값은 임시로 설정)
    public static String EMP_NAME = "";     // 이름 (초기값은 임시로 설정)

}